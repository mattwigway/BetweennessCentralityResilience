```{julia}
using BetweennessCentralityResilience, MetaGraphsNext, Plots, Graphs, GeoDataFrames
```

```{julia}
grid = make_graph()
grid_vids = make_grid!(grid, (0, 0), 9, 9)
rem_vertex!(grid, code_for(grid, grid_vids[5, 5]))
grid[grid_vids[4, 4], grid_vids[6, 6]] = EdgeData(2 * sqrt(2))
```

```{julia}
plot(graph_to_gdf(grid).geom)
```

```{julia}
discon = make_graph()
main_vids = make_grid!(discon, (0, 0), 
```

```{julia}
nvs = nv(G)
target_betweenness = 256 # TODO extract from grid

# n1 * n2 = target_betweenness
# To keep the graph the same size, n1 + n2 = nvs
# Therefore, n1 * (
```