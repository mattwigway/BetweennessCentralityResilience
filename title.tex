$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$$if(subtitle)$\\$subtitle$$endif$}
$endif$

$for(by-author)$
  \author{$by-author.name.literal$}
  $if(by-author.affiliations)$
    $for(by-author.affiliations)$
      \affil{%
        $if(by-author.affiliations.department)$
          $by-author.affiliations.department$, 
        $endif$
        $if(by-author.affiliations.name)$
          $by-author.affiliations.name$
        $endif$
      }
    $endfor$
  $endif$
$endfor$

\date{$date$}
